# Showcase video

The permanent muted showcase loop lives at `secretary-showcase.mp4`. Replace that file in place whenever the final footage changes; the Hub automatically plays it muted, inline, and on an endless loop.

The scroll-driven address clip lives at `scroll-story.mp4`. It reads the file's real duration at runtime, so a replacement five-second clip will automatically map its full timeline to the scroll section.
